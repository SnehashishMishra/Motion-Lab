"use client";

import React from "react";
import { motion } from "motion/react";

function Content() {
  return (
    <div className="[perspective::1000px] [transform-style::preserve-3d] min-h-[calc(100vh-80px)] w-full bg-neutral-900 flex items-center justify-center bg-dot-pattern"></div>
  );
}

export default Content;
